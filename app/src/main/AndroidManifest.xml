<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moderrr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditLocation.EditLocation"></activity>
        <activity android:name=".AddLocation.AddLocationActivity" />

        <activity android:name=".PermissionActivity.PermissionActivity" />

        <activity android:name=".AboutUs.AboutUsActivity" />
        <activity android:name=".ContactUs.ContactUsActivity" />
        <activity
            android:name=".Setting.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".Location.LocationActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDomy6SVbfrtw2E8onL7FQ08Vt3nQusWlw" />

        <receiver android:name=".LocationService" />

        <receiver android:name=".LocationService" />
    </application>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>